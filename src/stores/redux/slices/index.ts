import selectedIconReducer, {
  setSelectedIcon,
  clearSelectedIcon,
} from "./selectedIcon";

import layoutReducer, { setLayout } from "./layout";

export {
  selectedIconReducer,
  setSelectedIcon,
  clearSelectedIcon,
  layoutReducer,
  setLayout,
};
